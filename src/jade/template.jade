doctype html
html
	head
		block setup

		//- title
		title!= title

		//- SEO
		meta(charset="utf-8")
		meta(name='keywords', content='')
		meta(name='description', content='')

		//- responsive
		meta(name='viewport', content='width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, user-scalable=no')

		//- facebook
		meta(property="og:site_name", content="Book of Negroes")
		meta(property="og:title", content="Book of Negroes")
		meta(property="og:type", content="tv_show")
		meta(property="og:url", content="")
		meta(property="og:image", content="")
		meta(property="og:description", content="")

		//- mobile
		meta(name="apple-mobile-web-app-capable", content="yes")
		meta(name="apple-mobile-web-app-status-bar-style", content="black")
		link(rel="apple-touch-icon", href="")
		link(rel="apple-touch-icon", sizes="57x57", href="")
		link(rel="apple-touch-icon", sizes="72x72", href="")
		link(rel="apple-touch-icon", sizes="114x114", href="")

		//- favicon
		link(rel="icon", type="image/png", href="")
		
		//- css
		link(rel="stylesheet", href="#{cssPath}", type="text/css")

	body
		block content

		script.
			//- typekit async
			(function() {var config = {kitId: '#{locals.env.typekitID}'};var d = false;var tk = document.createElement('script');tk.src = '//use.typekit.net/' + config.kitId + '.js';tk.type = 'text/javascript';tk.async = 'true';tk.onload = tk.onreadystatechange = function() {var rs = this.readyState;if (d || rs && rs != 'complete' && rs != 'loaded') return;d = true;try { Typekit.load(config); } catch (e) {}};var s = document.getElementsByTagName('script')[0];s.parentNode.insertBefore(tk, s);})();
			
			//- FB
			window.fbAsyncInit = function() {FB.init({appId: '#{locals.env.fbID}', xfbml: true, version: 'v2.1'});};(function(d, s, id){var js, fjs = d.getElementsByTagName(s)[0];if (d.getElementById(id)) {return;}js = d.createElement(s); js.id = id;js.src = "//connect.facebook.net/en_US/sdk.js";fjs.parentNode.insertBefore(js, fjs);}(document, 'script', 'facebook-jssdk'));
			
			//- config for each env
			var Global = {
				host: '#{locals.env.host}',
				fbID: '#{locals.env.fbID}'
			}
		
		//- JS
		script(src="//cdnjs.cloudflare.com/ajax/libs/jquery/2.1.1/jquery.min.js")
		script(src="//cdnjs.cloudflare.com/ajax/libs/gsap/1.13.2/TweenMax.min.js")
		script(src="//cdn.jsdelivr.net/hammerjs/2.0.4/hammer.min.js")
		script(src="../js/lib.min.js")
		script(src="#{jsPath}")